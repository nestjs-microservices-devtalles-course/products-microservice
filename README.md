# Products microservice

## Dev

1. Clone repo
2. Install dependencies `pnpm i`
3. Create a `.env` file based in .`env.template`
4. Run prisma migration with `pnpx prisma migrate dev`
5. Run server with `pnpm start:dev`
